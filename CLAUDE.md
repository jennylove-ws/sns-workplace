# 돈의 궤적 — Remotion 프로덕션 가이드

## 절대 규칙: 이미 전달된 파트는 요청 없이 손대지 않는다

파트1~24는 사용자가 이미 리뷰하고 승인해서 전달받은 결과물이다. 새 파트 작업 중에
과거 파트의 패턴/컴포넌트를 참고하는 것은 괜찮지만, 코드나 렌더 결과를 임의로
다시 고치지 않는다. 과거 파트를 수정해야 한다면 반드시 먼저 물어보거나, 사용자가
명시적으로 "N번 다시 고쳐줘"라고 요청했을 때만 한다.

## 동적 배경 요소 사용 원칙 (2026-08-19 피드백 반영)

### 언제 "보조 동적 요소"가 필요한가
내레이션 문장을 그대로 화면 중앙에 다시 띄우기만 하는 비트(사진도, 그래프도,
아이콘도, 체크리스트도 없이 텍스트 fade-in만 있는 화면)는 지루하다. 이런 경우에만
가벼운 보조 동적 요소를 추가한다.

### 언제 추가하면 안 되는가
비트에 이미 사진(SlideImage), 차트(CrashChart), 아이콘 애니메이션, 체크리스트,
카드 뒤집기 등 다른 시각 요소가 있다면 그 위에 파형/이퀄라이저 같은 배경을 겹쳐
넣지 않는다. 이미 움직이는 요소가 있는데 배경까지 움직이면 산만하고 어지러워
보인다. "단순·정적"과 "복잡·산만" 사이의 균형을 지킨다.

### "이퀄라이저"의 정확한 의미
사용자가 말하는 이퀄라이저는 사인파(AnimatedWaveform)가 아니라, 음악 플레이리스트
앱에서 보이는 것처럼 **작은 세로 막대들이 위아래로 통통 튀는** 비주얼이다.
`src/components/AudioBars.tsx`가 이 스타일의 컴포넌트다.

### 같은 효과 반복 금지
한 영상(파트) 안에서, 그리고 여러 파트에 걸쳐 같은 보조 동적 요소만 반복해서 쓰면
그것대로 지루하다. AnimatedWaveform(사인파)만 계속 쓰지 말고, 아래처럼 다양한
보조 요소를 상황에 맞게 섞어서 쓴다:

- `AnimatedWaveform` — 사인파 3줄. 차분하고 잔잔한 톤에 어울림.
- `AudioBars` — 세로 이퀄라이저 막대. 리드미컬하고 긴장감 있는 톤에 어울림.
- 작은 파티클/점 드리프트, 은은한 그리드 라인 이동, 배경 그라디언트 펄스 등
  필요하면 새로 만들어도 된다 — 다만 항상 "가볍고 보조적"이어야 하며 텍스트
  가독성을 해치면 안 된다.

같은 파트 안에서 텍스트 전용 비트가 2개 이상이면 서로 다른 보조 요소를 쓴다.

## 씬 구축 파이프라인 (요약)

1. `src/data/subtitles.generated.ts` / `audioParts.generated.ts`에서 해당 파트의
   실측 자막 구간(프레임)을 조회해 비트 경계를 잡는다.
2. `docs/tts-script.md`에서 원문 내레이션을 확인해 비트별 시각 아이디어를 정한다.
3. `src/scenes/PartN Scene.tsx`를 작성한다. 각 파일은 `Centered`/`Caption` 로컬
   헬퍼를 자체적으로 재정의하는 기존 관례를 따른다.
4. `src/Composition.tsx`에 `PARTN_FROM = PART(N-1)_END`로 이어붙이고,
   `partStartFrame(N)`과 비교해 1프레임 이상 차이 나면(실제 삽입 갭이면) 원인을
   확인한다 — 반올림으로 인한 ±1프레임 오차는 무시해도 된다.
5. `npx tsc --noEmit`으로 타입 체크 → 대표 프레임 스틸컷으로 각 비트 확인 →
   해당 파트 프레임 구간만 렌더 → 커밋/푸시 → 사용자에게 전달.

## 이미지 재사용

파트마다 실사 이미지를 최소 1컷은 넣는다(전혀 안 쓰면 사용자가 지적함). 같은
이미지는 프로젝트 전체에서 최대 2번까지만 재사용한다. 어떤 이미지가 어느 파트에
쓰였는지는 `grep -rn "staticFile('images/" src/scenes/*.tsx`로 확인한다.
